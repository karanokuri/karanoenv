name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: windows-2019

    steps:
    - uses: actions/checkout@v2
    - name: Run setup
      run: .\setup.ps1

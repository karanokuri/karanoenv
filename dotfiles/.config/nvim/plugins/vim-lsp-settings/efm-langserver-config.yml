version: 2
root-markers:
  - .git/

tools:
  prettier: &prettier
    format-command: 'prettier --stdin-filepath ${INPUT}'
    format-stdin: true

languages:
  typescript:
    - <<: *prettier
  typescriptreact:
    - <<: *prettier
